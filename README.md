# html-css

## First lesson

## what to do for it to work and so we can download the first version
git -v
node -v
npm -v

git init
git config --global user.name
git config --global user.email
git branch -m main
git remote add origin <github link>
git pull origin main

## upload to cloud
git add .
git commit -m "second upload"
git push origin main

## how to download to my pc
git init
git branch -m main
git remote add origin